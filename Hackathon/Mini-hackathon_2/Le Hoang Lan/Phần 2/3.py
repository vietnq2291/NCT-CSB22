import turtle
list = ['blue', 'red', 'teal', 'green']
print(*list)
for x in list:
    turtle.color(list[x])    
    turtle.forward(100)
    turtle.penup
    turtle.forward(50)
    turtle.pendown
